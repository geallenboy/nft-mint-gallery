# contracts

Foundry project for `SimpleCollectible` (minimal ERC-721).

```bash
curl -L https://foundry.paradigm.xyz | bash
foundryup
cd contracts
forge install foundry-rs/forge-std --no-commit
forge build
forge test
```
